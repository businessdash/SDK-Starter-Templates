---
/**
 * Service-area index. Enumerates every (service × area) variant of the
 * `service-area` parallel page via `parallelPages.listVariants("service-area")`
 * and links to each materialised URL `/services/[service]/[area]`.
 *
 * This is the SSR-friendly substitute for `getStaticPaths` under
 * `output: "server"` — the platform owns the cartesian fan-out; we just list
 * what it returns. Degrades to an empty state when BD is unconfigured.
 */
import { bd } from "../../lib/bd";
import {
	isSuspensionError,
	suspendedResponse,
} from "../../lib/bd-suspension";
import Layout from "../../layouts/Layout.astro";

export const prerender = false;

type Variant = Record<string, string>;

let variants: Variant[] = [];
let suspended = false;

if (bd) {
	try {
		const res = await bd.parallelPages.listVariants("service-area");
		variants = res?.variants ?? [];
	} catch (err) {
		if (isSuspensionError(err)) suspended = true;
		else variants = [];
	}
}

if (suspended) return suspendedResponse();

function label(slug: string): string {
	return slug
		.split("-")
		.map((w) => w.charAt(0).toUpperCase() + w.slice(1))
		.join(" ");
}
---

<Layout
	title="Service areas — Your Business"
	description="Every service we offer, in every area we serve. Programmatic pages generated from BD."
>
	<section class="bd-section" id="service-areas">
		<div class="bd-section__lead">
			<span class="bd-section__eyebrow">Where we work</span>
			<h2 class="bd-section__title">Service areas</h2>
			<p class="bd-section__sub">
				Each link is a programmatic page generated from the
				<code>service-area</code> parallel page in <code>bd.config.ts</code>.
			</p>
		</div>

		{
			variants.length === 0 ? (
				<div class="bd-empty">
					No service-area pages yet. Add services + service areas in BD, then
					run <code>npm run sync-schema</code> so the platform fans them out.
				</div>
			) : (
				<div class="bd-grid-3">
					{variants.map((v) => (
						<a
							class="bd-card service-area-link"
							href={`/services/${v.service}/${v.area}`}
						>
							<h3>{label(v.service ?? "")}</h3>
							<span>in {label(v.area ?? "")}</span>
						</a>
					))}
				</div>
			)
		}
	</section>
</Layout>

<style>
	.service-area-link {
		padding: 1.25rem 1.5rem;
		display: flex;
		flex-direction: column;
		gap: 0.3rem;
	}
	.service-area-link h3 {
		font-size: 1.05rem;
	}
	.service-area-link span {
		color: var(--text-muted);
		font-size: 0.9rem;
	}
	code {
		font-family: var(--mono);
		font-size: 0.85em;
		background: var(--accent-bg);
		color: var(--accent);
		padding: 0.1em 0.35em;
		border-radius: 0.35rem;
	}
</style>
